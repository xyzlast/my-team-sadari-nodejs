div(style="text-align:right;margin-bottom:30px")
  h1 {{date}}
  button(type="button" class="btn btn-primary" ng-click="addNewGame()") Game 추가

h2(ng-show="games.length == 0") 게임이 진행되지 않았습니다.
div(ng-repeat="game in games")
  fieldset
    legend(style="text-align:right") {{game.winner}}({{game.number}})
      button(type="button" class="btn btn-info" ng-click="edit(game.id)") 수정
    ul
      li 내용 : {{game.cost}}
      li 비고 : {{game.description}}
    table(class="table table-condensed")
      thead
        tr
          th 이름
          th 번호
      tbody
        tr(ng-repeat="player in game.players", ng-class="player.number == game.number ? 'success' : 'none'")
          td {{player.playerName}}
          td {{player.number}}